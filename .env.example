# Canonical public origin for QR codes.
# Production builds use .env.production (committed to git).
# Override locally by creating .env or .env.local (both gitignored).
VITE_PUBLIC_SITE_URL=https://tatual.vercel.app
